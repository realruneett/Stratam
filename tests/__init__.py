"""Test package for the demand-prediction-overhaul pipeline.

Holds shared pytest fixtures (``conftest.py``) and reusable Hypothesis
strategies (``strategies.py``) that generate synthetic frames mirroring the
raw competition schema (pre-feature-engineering).
"""
